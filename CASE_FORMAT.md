# 시공사례 올리는 형식 요약 (sinkmaster.co.kr 기준 → doormaster.co.kr 에도 그대로 적용)

> doormaster.co.kr 은 폴더 자동 생성이 아니라 정적 HTML 이다. 폴더·제목.txt 대신 `case-{종류}-{지역}.html` 한 장을 만들고,
> 허브 페이지(door-fix / film / door-repair …)의 사례 목록 · 메인 갤러리 · 지역 페이지에 링크를 건다. 본문 뼈대·제목 규칙·사진 규칙은 아래와 동일.
> 배포는 `.\deploy.bat 메시지` 한 줄.

다른 홈페이지에도 이 형식 그대로 올린다. 대화창에 통째로 붙여넣고 시작하면 된다.

---

## 1. 진행 순서 — 절대 바꾸지 않는다

1. **키워드 후보를 5개씩 먼저 드린다** → 사장님이 네이버 키워드도구로 검색량 확인
2. 같은 계열 사례가 이미 있는지 **사이트맵(`/sitemap-0.xml`)으로 확인** → 겹치면 가지 나누기
3. 사진 가공 (원본 절대 그대로 안 씀)
4. 검색량 받은 뒤 **제목 3개 제안 + 추천 1개**
5. 본문 작성 → 키워드 횟수 세기
6. **Claude가 사장님 PC 폴더에 직접 넣는다** → 사장님은 `빌드하고 푸시해줘` 한 줄만
7. 사이트맵으로 올라간 것 확인 → 구글 색인 요청 (하루 4~5건 한도) → 네이버는 사장님 직접

검색량 받기 전에는 제목도 본문도 쓰지 않는다.

---

## 2. 폴더 형식

```
저장소루트/
  시공사례/
    지역_작업종류/          ← 언더바 하나, 공백·특수문자 금지
      제목.txt
      1.jpg                ← 시공 전
      2.jpg                ← 시공 후
      3.jpg                ← 작업 중 (선택, 원인이 드러난 장면)
```

폴더명 예: `화성_사각싱크볼수전교체`, `병점_가스레인지교체`

---

## 3. 제목.txt 형식

```
[제목 한 줄]
(빈 줄)
[본문]
```

본문 안 기호가 자동으로 HTML이 된다. 기호를 바꾸면 구조가 깨진다.

| 기호 | 결과 |
|---|---|
| `■ 소제목` | h3 |
| `✔ 항목` | 불릿 목록 |
| `1. 항목` | 번호 목록 |
| `Q.` / `A.` | 강조 문답 + FAQ 구조화 데이터 자동 생성 |

---

## 4. 제목 규칙

```
지역 + 검색량 높은 키워드 + 구체적 특징 (25~40자)
```

- 지역명 맨 앞
- 앞부분은 검색어를 잡고, 뒷부분은 다른 글과 구별시킨다
- 같은 작업 사례가 이미 있으면 **기존 글이 앞세운 말을 피하고** 이번 현장만의 것을 앞세운다
- "~했어요" 같은 검색 기여 0인 말 금지
- 표기 변형 확인 (가스렌지 20 vs 가스레인지 2,000 — 100배 갈렸다)
- 검색량 커도 그 말을 치는 사람이 손님이 아니면 안 쓴다 (제품명·자재명·브랜드명)

실제 예:
- `화성 싱크대 수전 교체 + 사각 싱크볼 교체 — 상판 교체 없이 구멍만 다듬어 2시간`
- `병점 가스레인지 교체 설치 — 단종된 구형 대체, 싱크대 상판 타공 현장 맞춤 1시간`
- `수원 에어컨 구멍 메우기 — 배관 철거 후 벽 구멍, 아이소핑크 단열까지 감쪽같이`

---

## 5. 본문 뼈대 (공백 제외 1,100~1,500자)

```
[답 선언 리드 2~3문장]
"○○에서 연락 주셨습니다"로 시작하지 않는다.
"싱크볼이 낡았다고 상판까지 바꿀 필요는 없습니다"처럼 결론부터.
마지막 줄에 지역 + 인원 + 시간.

■ 현장 상태
✔ 보이는 것이 아니라 확인한 것 3~4개

■ 왜 이 방법인가 / 그냥 ○○하면 안 되는 이유
판단의 근거. 타 업체 비난 아님.

■ 손님이 가장 많이 막히는 지점
전화 오게 만드는 대목. 손님이 직접 해보다 멈추는 자리,
제품만 사 뒀다가 안 맞는 자리.

■ 작업 순서
1.~6. 그 현장의 실제 작업. 빈 템플릿 돌려쓰기 금지.

■ 작업 결과
✔ 3~4줄. 마지막 줄 = 지역, 1명 ○시간 작업으로 당일 마무리

■ 자주 묻는 질문
Q. / A. 3~4개. 손님이 전화로 실제 묻는 말 그대로.
```

**키워드 반복 상한: 핵심 키워드 본문 3~5회, 최대 7회.** 쓰고 반드시 센다.

```python
import re
b = open('제목.txt', encoding='utf-8').read().split('\n', 1)[1]
print('공백제외', len(re.sub(r'\s', '', b)))
for k in ['싱크볼', '수전', '상판', '교체']:
    print(k, b.count(k))
```

**사장님이 말한 사실만 쓴다.** 인원·시간·수치를 안 들었으면 여쭙고, 답 없으면 그 문장을 뺀다.
사진 보고 크기를 추정해 쓰지 않는다. 원인을 지어내지 않는다.

---

## 6. 사진 규칙

- **원본을 절대 그대로 올리지 않는다.** prep.py로 가공 (EXIF 삭제, 기울기 교정, 크롭, 색 변형, 미세 노이즈)
- 가공 후 **원본과 pHash 차이 8 이상** 확인. 미달이면 다른 영역으로 미리 잘라내고 다시
- **개인정보 모자이크**: 얼굴, 거울·유리 반사, 문패, 택배 송장, 다른 업체 스티커 전화번호, 제품 바코드
- 모자이크는 축소→확대→블러 (블러만으로는 약함)
- 좌표 잡기 전에 `ImageOps.exif_transpose` 먼저
- **홈페이지는 2~3컷만.** 나머지는 블로그로
- **한 현장 사진은 한 채널에만.** 블로그·다른 홈페이지와 같은 컷 금지 (네이버 강등 원인)

---

## 7. 사이트 간 규칙

- **같은 현장을 두 홈페이지에 올리지 않는다.** 사장님 사이트끼리 같은 글이면 구글이 하나를 버린다
- 형식은 같아도 되지만 현장은 사이트마다 다르게
- 주방·문·복원 = sinkmaster / 타일 = tilemaster / 욕실 = 튼튼욕실수리센터

---

## 8. 다 넣은 뒤 사장님께 드리는 말 (이것뿐)

```
폴더 넣었습니다. Claude Code에 이것만 치세요 →  빌드하고 푸시해줘
```

+ 제목 / 본문 글자 수 / 키워드 횟수 / 사진 해시 차이 / 모자이크한 곳 한 줄씩.
